<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default configuration for all domains - use system certificates -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow cleartext (HTTP) traffic for IP address video streaming -->
    <!-- This bypasses SSL issues with Android WebView and Let's Encrypt certificates -->
    <domain-config cleartextTrafficPermitted="true">
        <domain>49.50.115.146</domain>
    </domain-config>
    
    <!-- Special configuration for app.sivakundalini.org and pub-*.r2.dev domains -->
    <!-- This trusts user-installed certificates in addition to system certificates -->
    <!-- Allows the app to work with self-signed or incomplete certificate chains -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">sivakundalini.org</domain>
        <domain includeSubdomains="true">r2.dev</domain>
        <trust-anchors>
            <!-- Trust system certificates -->
            <certificates src="system" />
            <!-- Also trust user-installed certificates (for testing/development) -->
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
    
    <!-- Allow CDN for HLS.js library -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">jsdelivr.net</domain>
        <domain includeSubdomains="true">cdnjs.cloudflare.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
    
    <!-- Explicit configuration for Google services -->
    <!-- Ensures Google Sign-In, Firebase, and related services work properly -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">gstatic.com</domain>
        <domain includeSubdomains="true">googleusercontent.com</domain>
        <domain includeSubdomains="true">firebaseapp.com</domain>
        <domain includeSubdomains="true">firebasestorage.app</domain>
        <domain includeSubdomains="true">cloudflarestorage.com</domain>
        <domain includeSubdomains="true">imagedelivery.net</domain>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </domain-config>
</network-security-config>
