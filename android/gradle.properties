org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# # ── Build speed optimizations ──────────────────────────────────────────────────
# # Keep Gradle daemon alive between builds (avoids JVM startup cost each time)
# org.gradle.daemon=true

# # Parallel project compilation
# org.gradle.parallel=true

# # Use build cache to reuse outputs from previous builds
# org.gradle.caching=true

# # Configure on demand — only configure projects that are needed
# org.gradle.configureondemand=true
